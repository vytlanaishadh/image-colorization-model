training:
  epochs: 50
  batch_size: 32
  learning_rate: 0.001
model:
  type: "UNet"
  input_shape: [256, 256, 3]
data:
  train_data_path: "data/train/"
  validation_data_path: "data/val/"
inference:
  output_path: "outputs/"
  save_model: true